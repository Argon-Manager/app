import { cleanup } from "@testing-library/react"

describe("RegistrationView", () => {
  afterEach(cleanup)

  test("success register user and set token to localstorage", () => {
    // render view
    // fill inputs
    //
  })
})
